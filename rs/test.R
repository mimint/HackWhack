# myfile.R

#* @get /mean
normalMean <- function(samples=10){
  data <- rnorm(samples)
  mean(data)
}

#* @get /sum
addTwo <- function(a, b){
  as.numeric(a) + as.numeric(b)
  as.numeric(a) - as.numeric(b)
" rfrfrfrfrfr"
}
	